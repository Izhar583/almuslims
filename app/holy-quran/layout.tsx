export const metadata = {
  title: "Holy Quran — AlMuslims",
  description: "Read all 114 surahs with Arabic, English, and Urdu translations.",
};

export default function QuranLayout({ children }: { children: React.ReactNode }) {
  return children;
}
